<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:SearchBarExemplo"
             x:Class="SearchBarExemplo.MainPage">

    <SearchBar x:Name="BarraPesquisa" SearchCommand="{Binding PesquisaCommand}" SearchCommandParameter="{Binding Valor, Source={x:Reference BarraPesquisa}}" />

    <StackLayout>
        <Label Text="Você Pesquisou:" />
        <Label Text="{Binding Valor}" />
    </StackLayout>
	
</ContentPage>
